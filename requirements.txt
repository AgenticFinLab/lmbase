# ---- Core (always required) ----
transformers
datasets
openai
python-dotenv
numpy
matplotlib
networkx
math_verify

# ---- Optional: local model inference (GPU servers only) ----
# Install manually when the server supports them:
#   pip install vllm qwen_vl_utils
# vllm
# qwen_vl_utils

# ---- Optional: FinAgent evaluation (lmbase.eval.finagent) ----
# pip install langgraph langchain-openai langchain-anthropic langchain-community langchain-core langchain-google-genai langchain-huggingface pydantic
